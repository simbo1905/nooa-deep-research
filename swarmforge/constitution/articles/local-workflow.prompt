# Local Workflow Rules

## QA Handoffs
- A QA handoff does not interrupt current work. Process it only through the
  normal helper-delivered queue.
- A QA result names one delivery gate and its exact validation. Do not merge or
  run a broad test suite merely because a QA wake-up arrived.
