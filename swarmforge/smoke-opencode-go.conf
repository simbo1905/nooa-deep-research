# Coder is first because the first role owns swarm cleanup.  It remains in the
# OpenCode TUI until the handoff arrives; the short-lived Claude sender is
# deliberately second.
window smoke-coder opencode smoke-coder task --model opencode-go/qwen3.7-plus
window smoke-specifier claude master task --model sonnet
